<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- RegisteredDnsServersDataSource reads ConnectivityManager.allNetworks/activeNetwork
         directly. The runtime-request rationale (there is none - this is a normal, non-dangerous
         permission) lives with the identical declaration in :app's manifest; this is just lint
         visibility for a library-scoped analysis, not a second source of truth. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
